Below is a **production-ready, robust, EM-friendly “triple fixer” prompt** you can use to clean the _rest_ of your dataset into **valid PromptCoT-2.0 triples**. This prompt enforces: _ correct **concept format** _ correct **rationale format** _ correct **problem format** _ perfect **alignment** across the three _ deletion of pedagogical fluff _ removal of soft-skills _ strict math-concept rewriting _ rationale compression into latent blueprint * guaranteed syntax-correct JSON output You can feed *any* dirty triple into it — including the messy ones from the authors — and it will return a triple that is EM-ready. --- # ✅ **PromptCoT-2.0 Triple Fixer Prompt** Copy/paste and run on every triple. --- ## **📌 PROMPT START** You will be given an object containing "concepts", "rationale", and "problem" fields. The data may be poorly structured, misaligned, overly verbose, pedagogical, logically inconsistent, or use soft “skills” instead of mathematical concepts. Your job is to **rewrite the entire triple** into a **valid PromptCoT-2.0 triple** with perfect internal coherence. --- # **🔧 REQUIRED OUTPUT FORMAT** You MUST output **only** a JSON dictionary with the following fields:
json
{
"concepts": [...],
"rationale": "string",
"problem": "string"
}
No commentary. No extra fields. Only valid JSON. --- # **📐 STRICT RULES FOR FIXING A TRIPLE** ### **1. Concepts** Rewrite the "concepts" field into **3–6 specific mathematical concepts** actually used by the final problem. Rules: * Use only **mathematical primitives**: “roots of unity”, “modulus constraints”, “polynomial factorization”, etc. _ Delete soft skills like: “logical reasoning”, “quantitative reasoning”, “estimation”, “nested operations”, “fractional arithmetic”, “deduction”. _ If the original triple mentions such skills, convert them into valid math concepts (e.g., replace “nested functions” with “solving equations derived from rational functions”). _ Concepts MUST match the final problem and rationale. --- ### **2. Rationale** Rewrite the "rationale" field into a **short (5–8 sentence)** latent construction blueprint. It must: _ Describe **how the concepts combine** to generate the final problem. _ Contain **no solution steps**, **no pedagogy**, **no Step 1/Step 2 meta narration**, **no hints**, **no teaching philosophy**, **no verifying difficulty**. _ Describe the **mathematical structure**, not the process of designing a problem. Examples of correct rationale structure: _ “Use roots of unity to create a discrete sample space.” _ “Introduce a rational functional constraint that becomes a polynomial after clearing denominators.” _ “Intersect the roots of this polynomial with the roots of unity to create a counting-based probability question.” --- ### **3. Problem** Rewrite the "problem" field into a **clean, well-posed contest problem** that: _ Uses **every** concept from "concepts". _ Naturally follows from the rewritten rationale. _ Is self-contained. _ Has a single correct numeric or symbolic answer. _ Has HMMT/AIME/Olympiad flavour if possible. _ Contains NO meta-language (“find the probability that there exists a k such that…”) unless mathematically correct. _ Defines randomness unambiguously when using probabilities. --- # **4. Alignment Requirements (Critical for EM)** Your rewritten triple must satisfy: _ Concepts → directly support the rationale _ Rationale → directly describes how the problem is constructed _ Problem → directly uses the concepts The triple must form a **tight chain**, not a loose association. --- # **5. Cleanup Rules** You MUST: _ Remove all fluff. _ Remove all teaching meta-narration. _ Remove talk about difficulty levels. _ Remove examples of verification and hints. _ Convert long multi-part rationales into a single coherent blueprint. --- # **6. NEVER:** _ Never include chain-of-thought or solutions. _ Never describe solving steps. _ Never output unaligned or soft concepts. _ Never output commentary outside the JSON. --- # **📌 Operation** Given any messy triple, rewrite it into a **valid PromptCoT-2.0 triple** that is aligned, compact, structural, and mathematically coherent. --- ## **📌 PROMPT END** ---

The data to fix:
